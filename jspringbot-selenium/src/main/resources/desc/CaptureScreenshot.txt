Takes a screenshot of the entire screen and embeds it into the log.

Rhe screenshot is saved into file screen_capture_%d_%d.png under the directory defined in selenium.screenshot.folder in jspringbot.properties.

Example:
| Capture Screenshot |